services:
  server:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: llm-memory-server:local
    # JWT key rotation (JWT_SIGNING_KEY_v1, v2, ...) を後から追加できるよう、
    # 個別列挙ではなく `.env` 全体を環境変数として流し込む。container は app
    # 起動時に `JWT_SIGNING_KEY_<kid>` を std::env::vars() で全列挙して読む。
    # `.env` 例 / 必須 key の説明はリポジトリ root の `.env.example` 参照。
    env_file: ../.env
    # Docker 内固定値の上書き (env_file より優先される)。
    environment:
      - DATABASE_URL=sqlite:///data/db.sqlite
      - BIND_ADDR=0.0.0.0:8080
    volumes:
      - data:/data
    expose:
      - "8080"
    # litestream が先に起動してから server を起こすことで、起動順を確定させる。
    # 副次効果として `docker compose down` 時の停止順が dependents 先 (= server
    # 先) になり、server の DB 書き込みが終わってから litestream を止める形に
    # なるので、最後の write が GCS replica に届かないまま停止する race を防ぐ。
    depends_on:
      - litestream
    restart: unless-stopped

  litestream:
    image: litestream/litestream:0.3.13
    # litestream.yml が `${LITESTREAM_BUCKET}` を参照するので、コンテナ環境に
    # 確実に注入する必要がある (compose の .env interpolation はファイルにしか効かない)。
    environment:
      - LITESTREAM_BUCKET=${LITESTREAM_BUCKET}
    command: ["replicate"]
    volumes:
      - data:/data
      - ./litestream.yml:/etc/litestream.yml:ro
    # GCE VM の instance service account を ADC で読むので、SA key ファイルは
    # マウントしない (gcp-sa.json をディスクに置く運用を避ける)。
    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    # HTTP/3 (UDP/443) は Claude Desktop には不要なので open しない。
    # tcp/80 は Let's Encrypt HTTP-01 challenge と http → https redirect 用。
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    environment:
      - PUBLIC_DOMAIN=${PUBLIC_DOMAIN}
    depends_on:
      - server
    restart: unless-stopped

volumes:
  data:
  caddy_data:
  caddy_config:
